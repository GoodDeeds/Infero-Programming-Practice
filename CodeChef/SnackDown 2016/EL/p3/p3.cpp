//numset
#include<bits/stdc++.h>
using namespace std;
int main()
{
	long long t,n,i,j,k,l;
	cin>>t;
	while(t--)
	{
		cin>>n;
		long long *arr=new long long[n];
		for(i=0;i<n;i++)
		{
			cin>>arr[i];
		}
		sort(arr,arr+n);
		
	}
	return 0;
}
