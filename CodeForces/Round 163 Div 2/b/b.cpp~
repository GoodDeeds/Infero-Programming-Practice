#include<bits/stdc++.h>
using namespace std;
int main()
{
	int n,t,lastg=-1;
	cin>>n>>t;
	string s;
	cin>>s;
	for(int i=n-1;i>=0;i--)
	{
		if(s[i]=='G')
		{
			lastg=i;
			break;
		}
	}
	if(lastg==-1 || lastg==0)
	{
		cout<<s<<endl;
		return 0;
	}
	for(int i=n-1;i>=0;i--)
	{
		if(s[i]=='B')
	}
}
