#include<bits/stdc++.h>
using namespace std;
int main()
{
	int n,x=0;
	cin>>n;
	string str;
	while(n--)
	{
		if(str.find("++",0)!=string::npos)
		{
			x++;
		}
		else
		{
			x--;
		}
	}
	cout<<x<<endl;
	return 0;
}
