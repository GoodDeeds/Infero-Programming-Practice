#include<iostream>
#include<cmath>
using namespace std;
int main()
{
	int t,a,b;
	cin>>t;
	for(int i=0;i<t;i++)
	{
		cin>>a>>b;
		int effexp=b%4;
		if(effexp==0)
		{
			effexp=4;
		}
		int slastdig=pow(a,effexp);
		int lastdig=slastdig%10;
		cout<<lastdig<<endl;
	}
	return 0;
}
