#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
int main()
{
	int t,nl,ns,i,j,k,l;
	cin>>t;
	for(i=0;i<t;i++)
	{
		cin>>nl;
		vector<int> last(nl);
		vector<int> laen(nl);
		for(j=0;j<nl;j++)
		{
			cin>>last[j]>>laen[j];
		}
		cin>>ns;
		vector<int> snst(ns);
		vector<int> snen(ns);
		for(j=0;j<ns;j++)
		{
			cin>>snst[j]>>snen[j];
		}
		
	}
}
